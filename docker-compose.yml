version: "3"

services: 
    db:
        image: rethinkdb
        internal: true
        volumes: 
            - ./rethinkdb_data:/data
        restart: always
    bot:
        build: "."
        environment: 
            NODE_ENV: production
        ports:
            - "8826"
        restart: always
        depends_on: [db]